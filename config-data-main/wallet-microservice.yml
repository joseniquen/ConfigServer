server:
  port: 8087

spring:
    application:
        name: wallet-microservice
    data:
        mongodb:
            host: localhost
            port: 27017
            database: BankWallet

eureka:
    client:
        serviceUrl:
            defaultZone: http://localhost:8761/eureka/
